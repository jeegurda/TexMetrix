{
  "name": "texmetrix",
  "version": "1.0.1",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=build/bundled/bundle.js --sourcemap=linked --minify",
    "serve": "esbuild src/index.ts --bundle --outfile=build/bundled/bundle.js --sourcemap=linked --serve --servedir=build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.17.8",
    "typescript": "^4.9.5"
  }
}
