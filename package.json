{
  "name": "texmetrix",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "esbuild index.ts --bundle --outfile=build/js/bundle.js --sourcemap=linked --minify",
    "serve": "esbuild index.ts --bundle --outfile=build/js/bundle.js --sourcemap=linked --serve --servedir=build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.17.8",
    "typescript": "^4.9.5"
  }
}
